#include "main.h"

/**
 * add - function that adds two integers @m and @n
 * @m: the first integer
 * @n: the second integer
 * Return: the result of addition
*/

int add(int m, int n)
{
int result;

result = m + n;
return (result);
}
