#include "main.h"

/**
 *positive_or_negative - function checks if number is positive or negative
 *
 *Return: 0 is success
 *
 */

void positive_or_negative(int i)
{
int = 0;

positive_or_negative(i);
return (0);
}
