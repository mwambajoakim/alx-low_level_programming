#include <stdio.h>

/**
 *main - entry point
 *
 *Return: always return 0
 *
 */

int main(void)
{
char a;
a = 'a';
do
{
putchar("%s\n", a);
}
while ((a < e) && (a > e) && (a < q) && (a > q));
putchar("\n");
return (0);
}
