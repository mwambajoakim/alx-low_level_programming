#include  "main.h"

/**
 *_strlen - Function that returns the length of a string
 *
 *@s: string to be tested
 *
 *Return: 0 is success
 *
 */

int _strlen(char *s)
{
char len;
int i;
for (i = 0; i != '/0'; i++)
{
len = i;
}
puts(len);
return(0);
}
